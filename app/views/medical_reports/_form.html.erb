<%= form_with(model: medical_report) do |form| %>
  <% if medical_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medical_report.errors.count, "error") %> prohibited this medical_report from being saved:</h2>

      <ul>
        <% medical_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :patient_id, style: "display: block" %>
    <%= form.text_field :patient_id %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
