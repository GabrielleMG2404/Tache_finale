<%= form_with(model: diagnostic) do |form| %>
  <% if diagnostic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(diagnostic.errors.count, "error") %> prohibited this diagnostic from being saved:</h2>

      <ul>
        <% diagnostic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :appointment_id, style: "display: block" %>
    <%= form.number_field :appointment_id %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.label :code_diagnostic, style: "display: block" %>
    <%= form.text_field :code_diagnostic %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
